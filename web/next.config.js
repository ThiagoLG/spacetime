/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', '192.168.1.12', 'github.com', 'linux-api-server.brazilsouth.cloudapp.azure.com' ]
  }
}

module.exports = nextConfig
